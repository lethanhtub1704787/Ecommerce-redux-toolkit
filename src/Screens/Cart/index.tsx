import {View, Text} from 'react-native';
import React from 'react';
import styles from '../Login/styles';

const Cart = () => {
  return (
    <View style={styles.container}>
      <Text style={{color: '#000000'}}>Cart Screen</Text>
    </View>
  );
};

export default Cart;
